#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main-entry point
*Return: wether the number is, greater than 5, less than 6 and not 0 or zero
*/
int main(void)
{
	int i;
	for(i = 'a'; i <= 'z'; i++)
	putchar(i);
	
	return(0);
}
